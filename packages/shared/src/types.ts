export type PropertyRecord = {
  addressText: string;
  priceText: string;
  priceNumber: number;
  href: string;
  created: string;
  broadband?: string;
  imageUrl?: string;
  houseArea?: string;
  landArea?: string;
};
